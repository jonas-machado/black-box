/**
 * Rotas publicas
 * @type {string[]}
 */

export const publicRoutes = ["/", "/register"];

/**
 * Para se logar e ser redirecionado
 * @type {string[]}
 */

export const authRoutes = ["/"];

/**
 * Para a autenticação
 * @type {string}
 */

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/pop";
